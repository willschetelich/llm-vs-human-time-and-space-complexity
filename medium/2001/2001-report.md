qid: 2001

LLM code:
Time: O(n log m) according to Claude
Space: O(n) according to Claude

Human code:
Time: O(n log(max(w,h) according to Claude
Space: O(n) according to Claude

big_o time (LLM): O(n)
big_o time (Human): O(n^k)

big-o and claude Disagree

Superior algorithm: Same
